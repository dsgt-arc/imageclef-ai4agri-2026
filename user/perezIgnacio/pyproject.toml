[project]
name = "ai4agri-Ignacio"
version = "0.1.0"
description = "DS@GT CLEF"
authors = [{name="Ignacio Perez"}]
readme = "README.md"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]